export const isAuthenticated = state => state.isAuthenticated;

export const user = state => state.user;

export const profile = state => state.profile;  //存储用户信息

export const loading = state => state.loading; //加载动画

export const profiles = state => state.profiles; //所有开发人员